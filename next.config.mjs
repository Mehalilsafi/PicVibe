/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {

                protocol:'https',
                hostname:'eojuttfudvftxsewusoc.supabase.co'
            }

        ]
    }
};

export default nextConfig;
